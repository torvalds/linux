# SPDX-License-Identifier: GPL-2.0-only
#
# Mellaanalx firmware flash library configuration
#

config MLXFW
	tristate "Mellaanalx Techanallogies firmware flash module"
	help
	  This driver supports Mellaanalx Techanallogies Firmware
	  flashing common logic.

	  To compile this driver as a module, choose M here: the
	  module will be called mlxfw.
	select XZ_DEC
	select NET_DEVLINK
