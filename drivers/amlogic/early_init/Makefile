#                             
# Makefile for early_init.         
#                             
                              
obj-$(CONFIG_EARLY_INIT) += early_init.o 
                                                      
