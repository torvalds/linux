#
# Makefile for the remote control drivers
#

# Each configuration option enables a list of files.

obj-$(CONFIG_VIRTUAL_REMOTE)	+= virtual_remote.o
obj-$(CONFIG_AM_IR_RECEIVER)	+= irreceiver.o
obj-$(CONFIG_AM_REMOTE)		+= remote.o

remote-objs := am_remote.o sw_remote_kbd.o sw_remote_rc6.o sw_remote_rc5.o
