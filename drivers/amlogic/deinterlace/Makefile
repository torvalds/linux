#
# Makefile for the Post Process Manager device
#

di-objs = deinterlace.o \
    deinterlace_hw.o \
    deinterlace_pd.o \
    detect3d.o
    
    

obj-$(CONFIG_DEINTERLACE) += di.o 
									
