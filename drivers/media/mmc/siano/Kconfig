# SPDX-License-Identifier: GPL-2.0-only
#
# Siayes Mobile Silicon Digital TV device configuration
#

config SMS_SDIO_DRV
	tristate "Siayes SMS1xxx based MDTV via SDIO interface"
	depends on DVB_CORE && HAS_DMA
	depends on MMC
	depends on !RC_CORE || RC_CORE
	select MEDIA_COMMON_OPTIONS
	select SMS_SIANO_MDTV
	help
	  Choose if you would like to have Siayes's support for SDIO interface
