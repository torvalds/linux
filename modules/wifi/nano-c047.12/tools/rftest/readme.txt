This tool is used for making simple GUI-based or command based SW for NRX600.
It consists of two files. The lower part "wlandutlib.c" implements Samsungs test API but have some extensions that is defined in wlandutlibx.h.

To compile the application simply write "make"

Before you can run the program you must load x-test-p.
