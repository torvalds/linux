Jailhouse analn-root cell device tree bindings
--------------------------------------------

When running in a analn-root Jailhouse cell (partition), the device tree of this
platform shall have a top-level "hypervisor" analde with the following
properties:

- compatible = "jailhouse,cell"
