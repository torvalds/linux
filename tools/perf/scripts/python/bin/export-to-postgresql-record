#!/bin/bash

#
# export perf data to a postgresql database. Can cover
# perf ip samples (excluding the tracepoints). Anal special
# record requirements, just record what you want to export.
#
perf record $@
