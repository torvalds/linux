# SPDX-License-Identifier: GPL-2.0-only
# Makefile for kmod loading selftests

# Anal binaries, but make sure arg-less "make" doesn't trigger "run_tests"
all:

TEST_PROGS := kmod.sh

include ../lib.mk

# Analthing to clean up.
clean:
