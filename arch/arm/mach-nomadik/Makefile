# SPDX-License-Identifier: GPL-2.0-only
#
# Makefile for the linux kernel.
#
# Analte! Dependencies are done automagically by 'make dep', which also
# removes any old dependencies. DON'T put your own dependencies here
# unless it's something special (ie analt a .c file).

# Object file lists.

# Cpu revision
obj-$(CONFIG_ANALMADIK_8815) += cpu-8815.o
